# Decisions and [VERIFY] results

Kept current during the build. Newest entries at the bottom of each section.
Status: **Open** (waiting), **Done** (decided / verified), **Changed** (spec updated to match).

## 1. Decisions

| # | Date | Decision | Why | Status |
|---|---|---|---|---|
| DEC-01 | 2026-09-19 | A full wildcard retrieve of `sf-line-dev` (2,771 files: SDO demo classes, Aura, LWC, pages, triggers, static resources) was in `force-app/`. It was **moved** to `archive/qa-org-retrieve-2026-09-19/`, not deleted. `force-app/` now holds only package source. | Everything in `force-app/` ships in the package. `archive/` is outside every package directory and is ignored by prettier, ESLint and Jest. It can be deleted once the user confirms it is not needed. | Done, waiting on user OK to delete `archive/` |
| DEC-02 | 2026-09-19 | The old POC `LineSendMessageCallout.cls` (in the archive) is **not** reused. | Hardcoded recipient, JSON built by string concatenation, a long-lived token in custom metadata, `System.debug` of responses. All conflict with D4, the HTTP conventions and the secret rules. | Done |
| DEC-03 | 2026-09-19 | `sfdx-project.json`: package dir `force-app` (package "LINE Connect", `0.1.0.NEXT`) + non-packaged `unpackaged`. `namespace` stays `""` and `packageAliases` is added by `sf package create`, both once the [HUMAN] M0 steps are done. | A placeholder namespace would break deploys. | Open (namespace, package name) |
| DEC-04 | 2026-09-19 | Scratch-org Site: created by `setup-scratch.sh` from `scripts/templates/site/` (metadata API format), with the org's admin username filled in at run time. Its home page `LineWebhookHome` lives in `unpackaged/`. | `CustomSite` holds org-specific usernames, so it can't be static source. The Site is subscriber-created (03 §4), so it is never packaged. | Done |
| DEC-05 | 2026-09-19 | Test reps are created by `scripts/apex/create-test-users.apex`, which picks a profile by permissions (standard Salesforce licence, Contact Edit, no Modify All Data), not by profile name. Rep A is `en_US`, Rep B is Thai (`th` / `th_TH`); both use `Asia/Bangkok`. | Same rule as `LineTestFactory` (07 §1). The Thai user exercises the TH labels. | Done |
| DEC-06 | 2026-09-19 | Code Analyzer runs `Recommended` + `Security` + `AppExchange` rules on `force-app` and fails on severity ≤ 2 (`scripts/code-analyzer.sh`, `npm run scan`). The **Flow engine is disabled** in `code-analyzer.yml`. | The package ships no Flows, and the Flow engine needs Python ≥ 3.10 (this machine has 3.9.6). Re-enable if a Flow is ever added. | Done |
| DEC-08 | 2026-09-19 | **Build before the Dev Hub/namespace (user-approved).** M1 and M2 are built and tested in **non-namespaced** scratch orgs created from `sf-line-dev` (already a Dev Hub): `DEVHUB=sf-line-dev ./scripts/setup-scratch.sh`. The Dev Hub, namespace, package name, `sf package create` and the "empty beta installs" proof must all be done **before M3's Beta 1**. Code stays namespace-agnostic (`LineNamespace` at runtime; no prefixes in source), and everything is re-tested in a namespaced scratch org when we switch. | Lets development start while the partner/namespace setup is pending. Scratch orgs don't bind the future package to `sf-line-dev`. | Done |
| DEC-09 | 2026-09-19 | **M1 schema details not fixed by 03** (all permanent once released): relationship names `LINE_OA_Configurations` (Assigned_Rep__c), `LINE_Conversations` (conversation → OA and → Contact), `Contacts` (Contact → OA), `LINE_Messages` (master-detail), `LINE_Messages_Sent` (Sender__c), `LINE_Error_Logs`. `LINE_Error_Log__c.Name` = Auto Number `LEL-{0000000}`. The required lookup `LINE_Conversation__c.LINE_OA_Configuration__c` uses delete **Restrict** (required lookups can't be Set Null), and the other lookups use Set Null. All picklists are **restricted**. Master-detail `writeRequiresMasterRead = false` (creating a message needs Edit on the conversation; outbound inserts run in system mode anyway). Settings numbers: `Poll_Interval_Seconds__c`, the expiry fields and `Message_Retention_Months__c` are Number(3,0); `Max_Download_Bytes__c` is Number(10,0). Numeric settings have **no metadata default**: `LineSettings` supplies them (03 §4); the two checkboxes carry their spec defaults. | Names and types must be right before the first release (09 §4). | Done |
| DEC-10 | 2026-09-19 | `Event.LINE_Sync_Key__c` is deployed as an **Activity** field (`objects/Activity/fields/`), so it also appears on Task. Its permission-set FLS entry must be `Activity.LINE_Sync_Key__c`: an `Event.` entry deploys but is silently dropped. | Platform: Event/Task custom fields live on Activity. | Done |
| DEC-11 | 2026-09-19 | Scratch definition turns on **Translation Workbench** (`languageSettings.enableTranslationWorkbench`). Without it, `translations/th.translation-meta.xml` is refused. The `LINE_Admin` tab points at an empty `LINE_Admin` app page until `lineAdmin` exists (M8). The validation-rule message is English only; Thai object/validation translations are part of M11. | Needed for Thai labels in scratch orgs; the tab must exist for the app in M1. | Done |
| DEC-12 | 2026-09-19 | **Apex class access** in the permission sets (`LineChatController`, `LineAdminController`, `LineWebhookResource`) is added when each class is built (M3–M8): a permission set can't reference a class that doesn't exist yet. | Deployability of M1 on its own. | Open (M3–M8) |
| DEC-13 | 2026-09-19 | **Proposed, needs user approval.** The platform forced three permission-set differences from 03 §6: (a) `LINE_Admin`: **Edit** on `LINE_Error_Log__c` (Delete requires Edit; field-level access stays read-only); (b) `LINE_Admin`: **View All** on `LINE_Error_Log__c` (the log is Private and mostly written by Automated Process, so without View All admins can't see it, contradicting 03 §4 "visible to LINE_Admin"); (c) `LINE_Webhook_Guest`: **Read** on `LINE_Webhook_Event__e` (Create requires Read; Read on a platform event only allows subscribing, which a guest session can't do in practice). All three are deployed in the scratch org. 03 §6 gets updated once approved. | Platform dependency rules; 03 §4 visibility requirement. | Open |
| DEC-14 | 2026-09-19 | **Proposed, needs user approval.** `LINE_Message__c.Public_Url__c` is specified as URL(1000), but Salesforce URL and Text fields max out at 255 characters, and `ContentDistribution` URLs can be longer. Options: (1) **Long Text Area(1000)** under the same API name (recommended: keeps the name, fits any URL, and the field is never used in filters); (2) URL(255), which risks truncation; (3) store the `ContentDistribution` Id (Text 18) and read the URL when needed. The field is **not created yet**; it's needed in M7. | Spec and platform disagree (CLAUDE.md: propose a fix before coding). | Open |
| DEC-15 | 2026-09-19 | **M2 core-service behaviour.** `LineApiClient`: one instance per channel (`forChannel` reads the stored secret; `withCredentials` is for registration/rotation); tokens cached per channel for the transaction, at most 10 min; push always sends `X-Line-Retry-Key` (new UUID unless the caller passes one to retry), and a 409 returns a result with `alreadyAccepted = true` plus the `sentMessages` LINE returns with it; content downloads use a 120 s timeout. `LineApiException`: `statusCode` 0 = no response; `isCredentialError()` = 401, or a rejected token request (the token endpoint answers **400** `invalid_client`, not 401); `isMonthlyLimit()` = 429 + "monthly limit"; `isRetryable()` = 0, 5xx, or 429 that isn't the monthly limit; `userMessage()` maps to Custom Labels. `LineLogger`: inserts in **system mode** (reps and Automated Process have no access to the log object), buffers at most 200 entries, never throws, and masks anything that looks like a bearer token or secret. `LineSettings`: poll interval minimum 2 s; `Max_Download_Bytes__c` capped at 10,000,000 (async heap); fallback owner accepted only if it is a User (005) or Queue (00G) Id. `LineTriggerHandler`: bypass by class name (namespace-independent). | Fills in behaviour 02 leaves open; all within the spec. | Done |
| DEC-16 | 2026-09-19 | `// NOPMD` on the `Authorization` header line in `LineApiClient` (rule `ApexSuggestUsingNamedCred`, High). | Named Credentials aren't used by design (D4): a principal per OA would need subscriber metadata for every OA. The token is short-lived and issued per transaction. | Done |
| DEC-17 | 2026-09-19 | **M3 inbound behaviour.** Webhook statuses beyond 02: **400** for an empty, oversized (>1 MB) or non-JSON body, or a missing `destination`; **500** if publishing fails, so LINE redelivers. The webhook writes **no log records** (junk traffic must not create data). The signature is compared **case-sensitively in constant time**: Apex `==` on Strings ignores case. Only the top-level `destination` is read before the signature check (streaming parser). Conversation `Name` = LINE user ID until the profile queueable sets the display name. **Owner** of a new conversation: active OA + active rep → rep; otherwise the fallback owner (an active user, or a queue that supports LINE Conversation); otherwise the rep; otherwise the OA record's owner. This lives in the inner class `LineInboundService.OwnerResolver` (no new top-level class). Existing conversations keep their owner; reassignment (M8) moves them. **Notifications:** one per conversation per batch, capped at 100 per batch, users only. **Retries:** only transient errors (row lock, retryable LINE errors), at most 3, via `EventBus.RetryableException`; otherwise log and acknowledge. `registerOA` upserts by Channel ID (re-registering updates the record and secret) and runs LINE's **webhook test asynchronously after the save**, because LINE's test call needs the saved OA to route; the result goes to `Webhook_Status__c`. | Fills in behaviour 02 leaves open. | Done |
| DEC-18 | 2026-09-19 | Code Analyzer suppressions with reasons: `sfge` (ApexFlsViolation, DatabaseOperationsMustUseWithSharing, High) on `LineOAConfigSelector.byBotUserIds` only, since the guest webhook must route in system mode (D2); `AvoidHardcodedCredentialsInFieldDecls` on the `x-line-signature` header-name constant. The inner class `OwnerResolver` declares `without sharing` itself: inner classes don't inherit sharing. | 09 §7 allows justified suppressions. The graph-engine finding's primary location is the query, so the marker must sit there. | Done |
| DEC-07 | 2026-09-19 | Prettier ignores `docs/`, `CLAUDE.md`, `README.md`, `manifest/`, `archive/` and `reports/`. `npm run lint` passes when there are no LWC files yet; `npm run test:unit` uses `--passWithNoTests`. | Keeps the hand-written spec tables as written, and lets the checks pass on an empty project. | Done |

## 2. [VERIFY] results

| Item (source) | Result | Date | Status |
|---|---|---|---|
| Scratch-org features needed for Sites (08, 09 §2) | **None.** A Developer-edition scratch org (no extra features) deployed and activated the `CustomSite` `LineWebhook` on the first try. The site domain is auto-provisioned: `SiteDetail.SecureUrl` = `https://<name>.scratch.my.salesforce-sites.com/linewebhook`, **including the path prefix**, so `Site_Base_Url__c` must keep the prefix. Tested empirically; the Salesforce docs site refuses automated fetches (HTTP 403). Also found: a scratch org has only **2 "Salesforce" licences** (1 = admin), so the test reps use "Salesforce Platform" (licence key `AUL`, which has Contact access). | 2026-09-19 | Done |
| Stateless channel access token (04 §1) | **Spec confirmed** against LINE's official OpenAPI definitions (`github.com/line/line-openapi`, `channel-access-token.yml`; the docs site renders client-side and can't be fetched): `POST https://api.line.me/oauth2/v3/token`, `application/x-www-form-urlencoded`, `grant_type=client_credentials`, `client_id`, `client_secret`; response `access_token`, `expires_in` (example 899 s), `token_type`; "only valid for 15 minutes but can not be revoked". Errors come back as **400** `{"error","error_description"}`. Also confirmed in `messaging-api.yml`: push `X-Line-Retry-Key` (UUID) and a 409 error body that includes `sentMessages`; the content and `/content/transcoding` endpoints on `api-data.line.me`; quota `type` none/limited + `value`; consumption `totalUsage`; webhook test result `success`, `statusCode`, `reason`, `detail`; set-endpoint max 500 characters. **Live check passed (2026-09-19)** with a real OA from the scratch org via `scripts/apex/line-smoke-test.apex`: a stateless token was issued from Channel ID + secret; `GET /v2/bot/info` returned `userId` (the webhook `destination`), `basicId`, `displayName`, `chatMode`; quota returned `limited 300` with usage 0; a push to the developer's own user ID was delivered (`sentMessages[0].id` returned). Remote Site Settings allowed the callouts. | 2026-09-19 | Done |
| Same LINE user ID across OAs under one Provider (D5) | — | | Open (M6) |
| Webhook through the Site guest user (D1–D3) | **Works (live, 2026-09-19, scratch org):** `registerOA` set the OA's webhook URL (`…scratch.my.salesforce-sites.com/linewebhook/services/apexrest/line/webhook`), LINE's `/webhook/test` returned `OK (200 OK)`. So the guest user with only `LINE_Webhook_Guest` can run the REST class, the system-mode OA lookup and protected-secret read work in guest context, and the signature check accepts LINE's real signature. | 2026-09-19 | Done |
| Automated Process: custom notifications + callout queueables (D3) | **Works, no subscriber config needed (live, 2026-09-19, scratch org).** Real phone → webhook (guest) → `LINE_Webhook_Event__e` → trigger runs as **Automated Process** (debug log: `Platform Event Trigger`, ~100 ms for 1 event: 5 SOQL, 2 upserts). `Messaging.CustomNotification.send()` completed with no exception. `LineCalloutQueueable` enqueued by Automated Process ran its profile callout and set the conversation name and picture. Sticker, image (placeholder) and Thai/English text messages were stored with the right types. Still to recheck in the **namespaced** QA install (Beta 1): the notification type lookup with a namespace. | 2026-09-19 | Done (namespaced recheck at Beta 1) |
| CustomNotificationType packageable and resolvable in subscriber orgs | Partial: `LINE_New_Message` deploys from source and is queryable by DeveloperName in a scratch org. Packageability can only be proven by the first beta build (the Metadata Coverage report was unreachable). | 2026-09-19 | Open (first beta, before M3) |
| Protected custom setting hidden from subscriber admins/API | Can't be tested unpackaged: protection only applies to installed managed packages. Check in the QA org after the first beta (Setup → Custom Settings, and a SOQL query as the admin). | 2026-09-19 | Open (first beta) |
| `line.me/R/oaMessage` pre-fill for non-friends, iOS + Android (D10) | — | | Open (M6) |
| Reply token validity vs PE → queueable latency | — | | Open (M6) |
| LINE accepts Salesforce `ContentDownloadUrl` for images/video | — | | Open (M7) |
| Guest user + package licensing | — | | Open (before licensing) |

## 3. Code Analyzer findings kept (justified)

| Date | Rule / severity | Where | Why it stays |
|---|---|---|---|
| 2026-09-19 | `ProtectSensitiveData` / Moderate (3) | `Assigned_Rep__c`, `Unique_Key__c`, `Public_Link_Expiry_Days__c`, `Update_Contact_Owner_On_Reassign__c` | Name-pattern heuristic ("key", "rep", "pub" …). None holds a secret; the only secret is `LINE_OA_Credential__c.Channel_Secret__c` in a protected custom setting. |
| 2026-09-19 | `FieldNamingConventions` / Moderate (3) | `LineApiModels.TokenResponse`, `ErrorResponse` (`access_token`, `expires_in`, `token_type`, `error_description`) | The names must match LINE's JSON keys for typed deserialization. |
| 2026-09-19 | `CognitiveComplexity`, `CyclomaticComplexity`, `ExcessiveParameterList`, `AvoidBooleanMethodParameters` / Moderate (3) | `LineApiClient`, `LineLogger`, `LineApiException.create`, `LineHttpMock`, `LineTestFactory` | Style thresholds. The client wraps 10 endpoints in one class by design (02 §2: "only `LineApiClient`" does HTTP); the logger overloads carry optional context. Revisit in M11 if they grow. |
| 2026-09-19 | `ApexDoc` / Low (≈90) | DTO fields, inner classes, test classes | ApexDoc is on every public method (CLAUDE.md); plain DTO fields and tests don't need it. |

## 4. Third-party code

| Library | Version | Source URL | Licence | Where |
|---|---|---|---|---|
| QR code library | TBD (M6) | | | static resource `qrcode` |
